# CAN_monitor Project Conventions

## 주석 언어

모든 주석은 한국어로 작성한다. 이는 인라인(`//`), 블록(`/* */`), XML 문서 주석(`///`), XAML 주석(`<!-- -->`)에 모두 적용된다.

- 새 주석을 작성할 때 한국어만 사용한다.
- 기존 영문 주석을 발견하면 한국어로 번역한다.
- 코드 식별자(클래스/메서드/속성명)는 원문(영문) 유지한다.
